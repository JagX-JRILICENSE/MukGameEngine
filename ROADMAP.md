# Muk Roadmap

## Done (v0.12)

### From prior “next 5”
1. GPU skinned VS path (`Shaders/Skin.hlsl` + SkinCB)
2. Bloom extract / strength pipeline
3. Procedural cubemap IBL
4. Prefab panel spawn
5. AI tool-use (focus/select/orbit/frame)

### Additional (~15)
- Trigger volumes, PIE script hot-reload, .mukscene load
- Console commands, project settings, grid snap, duplicate
- Selection highlight, camera orbit, game timers, string table
- FPS graph, day/night, Content Browser script/scene open

## Next

1. Compile Skin.hlsl into DX12 PSO and draw skinned meshes on GPU
2. Full-screen bloom blur pass on SceneRT
3. Sample cubemap in lit pixel shader
4. Multi-select + box select
5. Navmesh bake from scene meshes
